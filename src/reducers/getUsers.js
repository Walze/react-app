import * as axios from 'axios';

export default function getUsers(state = null, action) {

  // axios.get('https://httpbin.org/get').then(res => {
  //   console.log(res.data);
  // })

  return state;
}